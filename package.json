{
  "name": "zipline-cf-cache-cleaner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.6",
    "@cloudflare/workers-types": "^4.20240614.0",
    "typescript": "^5.5.2",
    "vitest": "1.5.0",
    "wrangler": "^4.10.0"
  }
}
